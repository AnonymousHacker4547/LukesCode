

  // All the variables we need right now 
  #define NUM 4
  double count;
  double gpa, total, grade;
  count=0;
  total=0;
  printf("A=4 A-=3.7 B+=3.3 B=3 B-=3.0 C+=2.3 C=2 C-=1.7 D+=1.3 D=1 F=0\n");
  while(count = grade) {
  printf("What is your grade?\n");
  scanf("%lf", &grade);
  Total = total + grade;
  }
 
  gpa = (grade) / 4;
  
 printf ("Your GPA is %f\n", gpa);
